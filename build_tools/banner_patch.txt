================================================================================
FILE: banner_patch.txt
ROLE: Add pre-processor status to print_banner()
LOCATION: prompt_expert_enhance.py, print_banner() function
================================================================================

CURRENT print_banner() output:
-------------------------------
==============================================================
   PROMPTURGY  —  Expert Prompt Enhancement Tool  v2.2
==============================================================
   Model A     : llama3:latest
   Model B     : qwen2.5:7b
   Synthesis   : qwen2.5:7b
   Temperature : 0.3
   Techniques  : 15 active / 173 available
   Output mode : FULL (12-section manifest)
   Internet    : ON   Web enrichment : ON   Streaming : ON
--------------------------------------------------------------


NEW print_banner() output after patch:
---------------------------------------
==============================================================
   PROMPTURGY  —  Expert Prompt Enhancement Tool  v2.2
==============================================================
   Model A     : llama3:latest
   Model B     : qwen2.5:7b
   Synthesis   : qwen2.5:7b
   Temperature : 0.3
   Techniques  : 15 active / 173 available
   Output mode : FULL (12-section manifest)
   Pre-process : ON   (llama3:latest)
   Internet    : ON   Web enrichment : ON   Streaming : ON
--------------------------------------------------------------


CODE PATCH for print_banner():
-------------------------------

Add these lines BEFORE the Internet line:

    use_pp = settings.get("use_pre_processor", True)
    pp_model = settings.get("pre_processor_model") or settings.get("model_a", DEFAULT_MODEL_A)
    pp_label = f"ON   ({pp_model})" if use_pp else "OFF"
    print(f"   Pre-process : {pp_label}")


POSITION in the function:
    ... (existing lines for Techniques and Output mode)
    print(f"   Pre-process : {pp_label}")          # ← NEW LINE HERE
    print(f"   Internet    : {inet}   Web enrichment : {web_st}   Streaming : {stream_st}")
    print("-" * 62)


VERSION BUMP:
-------------
Bump banner version from v2.2 to v2.3 when this feature is released:
    print("   PROMPTURGY  —  Expert Prompt Enhancement Tool  v2.3")

Also update the version constant if one exists in the file.
Check: grep -n "v2\." prompt_expert_enhance.py

================================================================================
